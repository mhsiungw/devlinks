type: 'application'
language: 'javascript'

tasks:
  dev:
    command:
      - 'next'
      - 'dev'
      - '-p'
      - '3001'

  start:
    command:
      - 'next'
      - 'start'

  build:
    command:
      - 'next'
      - 'build'

