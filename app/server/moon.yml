type: 'application'
language: 'nodejs'

tasks:
  dev:
    command:
      - 'DEBUG="development:*"'
      - 'nodemon'
      - './bin/www.js'

  seed:
    command:
      - 'node'
      - './bin/db/seed.js'
